# Build artifacts
node_modules/
dist/
target/

# Portable runtime data — never commit user models/ffmpeg/cache
data/

# PyInstaller intermediate output
src-tauri/binaries/worker-*
python-sidecar/dist/
python-sidecar/build/
python-sidecar/__pycache__/
python-sidecar/worker/__pycache__/
**/__pycache__/
python-sidecar/_pyinstaller_entry.py
python-sidecar/worker.spec

# Release-only artifacts (uploaded to GitHub Releases, not the repo)
*.dmg
*.app.tar.gz
*.app.tar.gz.sig
latest.json

# Tauri override config (regenerated by release.sh per build)
src-tauri/tauri.release.conf.json

# OS / editor noise
.DS_Store
.vscode/
.idea/

# Local backup .app copies (snapshots kept by user, not project source)
*.app/
*.app
icon-source.png
ambient_user/
